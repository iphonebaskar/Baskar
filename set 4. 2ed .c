#include<stdio.h>
#include<conio.h>
#include<string.h>
int main()
{
    char a[20];
    int j,count=1,c;
    printf("enter the string");
    gets(a);
    c=strlen(a);
    if(a[j]==' ')
    {
        count=count+1;
    }
    printf("the total words is %d",count);
getch();
}
